# pick-next スキル Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 「次に何をやるか」を対話で決定する `pick-next` スキルを `claude/skills/pick-next/` 配下に新設し、`linear-next` の優先度推奨機能を内包しつつ、新規テーマ作成・既存 Issue 選定・保留の 3 分岐に振り分けられるようにする。

**Architecture:** SKILL.md（入口・対話フロー）+ 7 つの references（責務別の詳細仕様）の 2 層構造。`linear-next` の handover/ブランチ/priority シグナルを `ranking-signals.md` に移植し、3 軸スコア（インパクト × モチベ × コスト）と組み合わせて候補を比較する。新規テーマ確定時は軽量 spec/plan を `docs/superpowers/{specs,plans}/` に書き出し、`Skill: create-issue` 経由で親 Issue + sub-issue を登録する。

**Tech Stack:** Markdown (SKILL.md, references), zsh (検証コマンド), 既存 Skill 連携 (`create-issue`, `handover`), Linear MCP / `gh` CLI, dotfiles の `setup.zsh` symlink 機構。

---

## File Structure

実装で触るファイル一覧。`claude/` 配下のものは `setup.zsh` の再帰展開で `~/.claude/` に自動 symlink される（auto memory 既知）。

| 種別 | パス | 役割 |
|------|------|------|
| Create | `claude/skills/pick-next/SKILL.md` | スキルの入口、対話プロトコル Step 0〜7 |
| Create | `claude/skills/pick-next/references/environment-detection.md` | 環境検出と grace degrade |
| Create | `claude/skills/pick-next/references/ranking-signals.md` | 既存 issue ランキングの内部スコア定義 |
| Create | `claude/skills/pick-next/references/score-axes.md` | 3 軸スコアの定義と対話テンプレ |
| Create | `claude/skills/pick-next/references/decomposition-guide.md` | sub-issue 分解の指針 |
| Create | `claude/skills/pick-next/references/spec-template.md` | 軽量 spec の完全テンプレ |
| Create | `claude/skills/pick-next/references/plan-template.md` | 軽量 plan の完全テンプレ |
| Create | `claude/skills/pick-next/references/decision-log-template.md` | 「保留」分岐の判断ログテンプレ |
| Modify | `CLAUDE.md` | 「Claude Code 設定」セクションに `pick-next/` の説明を追記 |

検証用に手動実行するコマンドのみで自動テストは無し（spec §14 通り、実運用で検証）。

---

## Task 1: ディレクトリ作成と SKILL.md スケルトン配置

**Files:**
- Create: `claude/skills/pick-next/SKILL.md` (スケルトンのみ)

- [ ] **Step 1: ディレクトリ作成**

```bash
mkdir -p /Users/goto/.dotfiles/claude/skills/pick-next/references
```

- [ ] **Step 2: SKILL.md スケルトンを書き出す**

Write ツールで `/Users/goto/.dotfiles/claude/skills/pick-next/SKILL.md` に以下を書く。

```markdown
---
name: pick-next
description: 「次に何をやるか」を対話で決定する。既存 active issue（Linear / GitHub）の優先度推奨、新規テーマの候補出しと 3 軸スコア比較、判断結果に応じて Issue 作成・既存 Issue 選定・保留の 3 分岐に振り分ける。「次に何やる？」「Linear/GitHub 確認して」「優先度教えて」「次の開発内容を相談したい」と聞かれたら必ず使う。引数なし or 任意のヒント文字列で起動。
argument-hint: '[hint] [--epic <issue-id>] [--all] [--axes <カスタム軸>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
  - Write
  - TaskCreate
  - TaskUpdate
  - ToolSearch
  - Skill
---

# pick-next

「次に何をやるか」を対話で決定する。Step 0〜7 を順に実行し、最後に 3 つの分岐（既存 Issue 選定 / 新規テーマ作成 / 保留）のいずれかに到達する。

## 対話プロトコル概要

詳細は references を参照しながら、各 Step を順に実行する:

- Step 0: 環境検出 + handover → `references/environment-detection.md`
- Step 1: 既存 active issue 取得 & ランク付け → `references/ranking-signals.md`
- Step 2: ヒアリング & 新規候補引き出し
- Step 3: 候補統合（既存 + 新規 + 推測）
- Step 4: 3 軸スコア & 比較 → `references/score-axes.md`
- Step 5: 1 つ選定 & 分岐判定
- Step 6A: 既存 Issue 確定（issue-dev 起動を案内）
- Step 6B: spec/plan 書き出し → create-issue 呼び出し → `references/spec-template.md` `references/plan-template.md` `references/decomposition-guide.md`
- Step 6C: 保留（判断ログ任意）→ `references/decision-log-template.md`
- Step 7: 完了報告

本ファイルは Task 9 で本文を完成させる。
```

- [ ] **Step 3: symlink を確認**

Run:
```bash
ls -la /Users/goto/.claude/skills/pick-next/
```

Expected: `SKILL.md` と `references/` が symlink 経由で見える（`setup.zsh` の再帰展開により、新ディレクトリは自動で symlink ターゲットに含まれる、auto memory 既知）。
もし見えない場合: `cd /Users/goto/.dotfiles && zsh setup/setup.zsh` を実行。

- [ ] **Step 4: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/SKILL.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next スキルのスケルトンを追加

「次に何をやるか」を決定する pick-next スキルのディレクトリと
SKILL.md スケルトンを配置する。本文は references が出揃った後の
別 commit で完成させる。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: environment-detection.md

**Files:**
- Create: `claude/skills/pick-next/references/environment-detection.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。

```markdown
# environment-detection

`pick-next` の Step 0 で実行する環境検出と grace degrade の仕様。

## 並列実行コマンド

どれか失敗しても他は続行する。失敗時は対応する候補ソースを「不在」として扱う。

```bash
# 現在のリポジトリ・ブランチ・未コミット変更
git rev-parse --show-toplevel 2>/dev/null
git branch --show-current 2>/dev/null
git status --porcelain 2>/dev/null | head -10

# 直近コミット（文脈把握用）
git log --oneline -20 2>/dev/null

# プロジェクト設定（tracker タイプ判定）
cat .claude/project.yml 2>/dev/null || true

# handover 未消費メモ（現プロジェクト・現ブランチ）
eval "$(${HOME}/.claude/skills/handover/scripts/resolve-path.sh)" 2>/dev/null
${HOME}/.claude/skills/handover/scripts/list-active.sh "${PROJECT_HASH}" "${BRANCH}" 2>/dev/null

# 中断再開検出: pick-next 由来の spec/plan があるか
find docs/superpowers/specs -name '*-design.md' -exec grep -l '^pick-next: true' {} \; 2>/dev/null | head -5
```

## tracker タイプ判定

`.claude/project.yml` を yq でパースする（yq が無ければ簡易 grep）。

```bash
TRACKER_TYPE=$(yq -r '.tracker.type' .claude/project.yml 2>/dev/null \
  || grep -E '^[[:space:]]+type:' .claude/project.yml 2>/dev/null | head -1 | awk '{print $2}')
```

得られる値: `linear` / `github` / 空（未設定）。

## Grace Degrade ルール

| 条件 | 挙動 |
|------|------|
| repo 外で起動 | コードベース読込みスキップ、対話続行 |
| `.claude/project.yml` 未存在 / `tracker.type` 空 | 既存候補ゼロで続行（新規候補のみ）、Step 6B 選択時に「`.claude/project.yml` を設定してから `/create-issue <spec> <plan>` を手動実行」と案内 |
| Linear MCP 認証エラー（`mcp__linear-server__list_issues` 呼び出しで失敗） | 「Linear MCP がエラー (xxx)。`/mcp` で接続確認、もしくは `linear auth login` の再実行が必要かも」と提示、既存候補ゼロで続行 |
| GitHub `gh auth status` NG | 「`gh auth login` が必要」と提示、既存候補ゼロで続行 |
| handover scripts 未存在（`~/.claude/skills/handover/scripts/list-active.sh` が無い） | handover 連携をスキップ、警告は出さない |

## 中断再開検出

`docs/superpowers/specs/*-design.md` のフロントマターに `pick-next: true` を含むファイルが見つかった場合:

1. 検出したファイルの `title` と `date` を一覧表示する
2. ユーザーに「続きから再開する？破棄して新規？」を確認
3. 「再開」→ 該当 spec/plan を読み込んで、対応する Step（Step 5 の選定済みテーマ）から続行
4. 「破棄」→ 何もしない（既存ファイルは残す、ユーザーが手動削除）

## 出力

Step 0 の終わりに、以下を Step 1 以降の判断材料として保持する:

- `REPO_ROOT`: リポジトリルート（または空）
- `BRANCH`: 現在のブランチ名
- `TRACKER_TYPE`: `linear` / `github` / 空
- `HANDOVER_NOTES`: 未消費 handover の一覧（issue 名・ブランチ名を含む文字列群）
- `RECENT_COMMITS`: `git log --oneline -20` の出力
- `RESUMING_SPEC`: 中断再開対象の spec パス（または空）
```

- [ ] **Step 2: ファイルが書けたことを確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/environment-detection.md
ls -l /Users/goto/.claude/skills/pick-next/references/environment-detection.md
```

Expected: 両方が存在し、後者は前者への symlink。

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/environment-detection.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next environment-detection.md を追加

Step 0 で実行する環境検出コマンド・tracker タイプ判定・grace
degrade ルール・中断再開検出ロジックを定義する。handover 連携
コマンドは linear-next と同形式で揃える。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: ranking-signals.md

**Files:**
- Create: `claude/skills/pick-next/references/ranking-signals.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。

```markdown
# ranking-signals

既存 active issue を「ランク付け」するときに使う**内部スコア**の定義。
スコアそのものはユーザーに表示せず、推奨理由を自然言語で出力する（`linear-next` から移植した設計判断）。

## tracker 別の Issue 取得

### Linear

`ToolSearch` で MCP ツール schema を読み込む:

```
ToolSearch query="select:mcp__linear-server__list_issues,mcp__linear-server__get_issue,mcp__linear-server__list_issue_statuses"
```

`list_issues` を以下フィルタで呼ぶ:

- `state`: "In Progress" / "Todo" / "Backlog" のいずれか（3 回呼ぶ or 全件取得して filter）
- `assignee`: `me` を優先、ヒットしなければ無指定で project 全体
- `project`: `.claude/project.yml` の `tracker.linear.project` 値、なければリポジトリ名から推測
- `limit`: 100

`--epic <issue-id>` 引数があれば `parentId: <issue-id>` でフィルタ。

### GitHub

```bash
# 自分が assignee の active issue
gh issue list --state open --assignee @me --limit 30 \
  --json number,title,labels,assignees,milestone,projectItems

# project がある場合は項目とその status も取得
gh project item-list <PROJECT_NUMBER> --owner <OWNER> --format json --limit 200
```

`--epic <issue-id>` 引数の挙動: GitHub には Linear の `parentId` のような明示 epic 概念が薄い。代わりに `gh issue view <epic-id> --json body` から「タスクリスト形式の sub-issue 番号」を抽出してフィルタする。抽出パターン: `^- \[[ x]\] #(\d+)`。

## スコアシグナル

各 issue にスコアを加算する。**最終スコアは表示しない**。

| シグナル | 加点 | 検出方法 |
|---------|------|---------|
| handover に該当 issue 名・ブランチ名が出現 | +100 | `HANDOVER_NOTES` を grep |
| 現ブランチ名と一致（gitBranchName） | +50 | Linear: `gitBranchName` フィールド / GitHub: ブランチ名に issue 番号が含まれるか |
| status = In Progress | +30 | tracker のステータス |
| priority = Urgent | +40 | Linear: priority=1 / GitHub: `priority:urgent` ラベル |
| priority = High | +20 | Linear: priority=2 / GitHub: `priority:high` ラベル |
| 親 epic の blocker が解消済み | +10 | 親 epic を取得し、前提 sub-issue が Done/closed か |
| 親 epic に未解決 blocker あり | -50 | 同上、前提 sub-issue が未完 |
| status = Backlog かつ親 epic 不明 | -10 | 親 epic 検出失敗 + Backlog |

合計スコア降順で上位 3〜5 件を「**既存候補**」として保持。タイブレークは `updatedAt` 新しい順。

`--all` 引数があれば 5 件で打ち切らず全件返す。

## 推奨理由（ユーザー向け）

スコアが付いた要因のうち最も加点が高いもの 1 つを「推奨理由」として自然言語で表現する。

| 主な加点要因 | 自然言語例 |
|-----------|----------|
| handover (+100) | 「中断作業の再開（handover 未消費メモあり）」 |
| 現ブランチ一致 (+50) | 「現在のブランチで進行中」 |
| Urgent priority (+40) | 「priority=Urgent」 |
| In Progress (+30) | 「着手中」 |
| High priority (+20) | 「priority=High」 |
| 親 epic blocker 解消 (+10) | 「依存先が解消済み、着手可能」 |

複数該当する場合は最も加点が高いもの優先。負の加点（blocker 未解消、Backlog）の場合は推奨理由ではなく「**注意**」として表記:

| 減点要因 | 自然言語例 |
|---------|----------|
| 親 epic blocker 未解消 (-50) | 「⚠ 依存先が未完、現状では着手不可」 |
| Backlog かつ親 epic 不明 (-10) | 「Backlog（着手前提が曖昧）」 |

## 設計判断

- **スコアは内部のみ**: ユーザーに数値を見せると「なぜ 50 点？」と無駄な議論になる。理由を自然言語で書く方が建設的（`linear-next` の設計判断を踏襲）
- **推測時間 (estimate) は出さない**: 既存 issue の estimate フィールドが空のとき、推測すると害が大きい。書くなら issue 側に estimate が入っているときだけ
- **ユーザーが選ぶ**: 自動で「これをやる」と断定せず、ランク付けして候補に出すまでが本 reference の責務。最終決定は Step 5 でユーザーが行う
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/ranking-signals.md
```

Expected: ファイル存在、内容は `linear-next` のシグナル定義と同等の表を含む。

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/ranking-signals.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next ranking-signals.md を追加

linear-next の優先度推奨ロジック (handover +100, 現ブランチ +50,
priority/blocker 加減点) を内部スコアとして移植する。Linear MCP
と GitHub gh CLI 両対応の取得方法も併記する。スコアそのものは
ユーザーに表示せず、推奨理由を自然言語で出力する設計を維持する。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: score-axes.md

**Files:**
- Create: `claude/skills/pick-next/references/score-axes.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。

```markdown
# score-axes

`pick-next` の Step 4「3 軸スコア & 比較」で使う 3 軸の定義と対話テンプレ。

## 3 軸の定義

| 軸 | 値 | 意味 |
|----|-----|------|
| **インパクト** | 高 (3) / 中 (2) / 低 (1) | KPI・ユーザー価値・サービス品質への効き |
| **モチベーション** | 高 (3) / 中 (2) / 低 (1) | 自分が今やりたいか |
| **コスト** | 小 (1d 以下) / 中 (2-5d) / 大 (1w+) | ざっくり実装工数 |

## 軸カスタム

`--axes <カンマ区切り>` 引数で 3 軸を上書きできる。例:

- `--axes インパクト,学習,コスト` — 学習目的の個人開発
- `--axes リスク,モチベ,コスト` — セキュリティ強化フェーズ
- `--axes リリースブロッカー度,モチベ,コスト` — リリース直前

カスタム軸の値の刻みは「高 (3) / 中 (2) / 低 (1)」を共通とする（コスト軸だけは例外で「小 / 中 / 大」を維持）。

## 対話テンプレ

候補数を `N` とすると、ユーザーに以下を順に確認する:

1. **各候補のインパクトを順に**: 「候補 1 のインパクトは 高/中/低 のどれですか？KPI やユーザー価値への効きで考えてください」
2. **各候補のモチベを順に**: 「候補 1 のモチベは 高/中/低 のどれですか？今いちばんやりたい順で考えてください」
3. **各候補のコストを順に**: 「候補 1 のコストは 小 (1d 以下) / 中 (2-5d) / 大 (1w+) のどれですか？ざっくりで OK」

各軸を一気に聞いてもよい。例: 「候補 1 はインパクト・モチベ・コストでそれぞれどう？」

候補が既存 issue の場合、ランキングシグナルの推奨理由を補助情報として提示する:

> 候補 1 (KISSA-55: 認証強化) は handover に未消費メモがある（中断作業の再開）。インパクト・モチベ・コストはどう評価しますか？

## 比較表のフォーマット

```
| # | 候補                       | タイプ | インパクト | モチベ | コスト | 推奨理由 / コメント                |
|---|---------------------------|-------|-----------|--------|--------|----------------------------------|
| 1 | KISSA-55: 認証強化         | 既存  | 高 (3)    | 中 (2) | 中 (3d) | handover に未消費メモあり         |
| 2 | コーヒーログ機能拡張        | 新規  | 中 (2)    | 高 (3) | 低 (1d) | 自分が一番使う、すぐ出せる         |
| 3 | KISSA-60: SEO 改善         | 既存  | 高 (3)    | 低 (1) | 中 (3d) | priority=High だが効果出るのが遅い |
```

## 判定原則

「**コストが許容範囲なら、インパクト × モチベの積が高いものを選ぶ**」を原則とする。具体的には:

1. ユーザーから「使える時間 / 期限」を Step 2 で聞いていれば、それを上限としてコスト「大」を除外できる
2. 残った候補で `インパクト × モチベ` を計算（数字部分の積）
3. 積の降順を「推奨順」として提示する
4. ただし**自動で 1 つに決めない**。ユーザーが Step 5 で最終判断する

**個人開発はモチベが落ちると進まない**ため、モチベ「低」の候補を機械的に推奨しない。インパクトが極端に高くても、モチベ「低」のときは「インパクトは高いがモチベが低い。本当にこれにしますか？」と確認する。

## 例

### 例 1: シンプル選択

```
| # | 候補                  | インパクト | モチベ | コスト | 積 |
|---|----------------------|-----------|--------|--------|-----|
| 1 | A                    | 3         | 3      | 1d     | 9   |
| 2 | B                    | 3         | 1      | 2d     | 3   |
| 3 | C                    | 2         | 2      | 1d     | 4   |
```

→ 推奨: A（積 9）。

### 例 2: モチベ確認が必要

```
| # | 候補                  | インパクト | モチベ | コスト | 積 |
|---|----------------------|-----------|--------|--------|-----|
| 1 | A: セキュリティ修正   | 3         | 1      | 3d     | 3   |
| 2 | B: 新機能            | 2         | 3      | 5d     | 6   |
```

→ A はインパクト最大だがモチベ低。B のほうが積は高い。
→ ユーザーに確認: 「A はインパクト高いがモチベ低、B はモチベ高い。今のフェーズだとどちらが妥当ですか？」

### 例 3: コスト超過で除外

ユーザーが Step 2 で「3 日しか時間が取れない」と言った場合:

```
| # | 候補                  | インパクト | モチベ | コスト | 採用判定 |
|---|----------------------|-----------|--------|--------|---------|
| 1 | A                    | 3         | 3      | 1w+    | 除外 (コスト超過) |
| 2 | B                    | 2         | 2      | 2d     | 候補 |
| 3 | C                    | 2         | 3      | 3d     | 候補 |
```

→ B vs C で比較。積 4 vs 6 → C 推奨。
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/score-axes.md
```

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/score-axes.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next score-axes.md を追加

3 軸 (インパクト × モチベ × コスト) の定義、対話テンプレ、比較
表フォーマット、判定原則 (積の降順 + モチベ低の機械的不採用) を
定める。--axes フラグでの軸カスタム仕様と 3 つの典型例も併記する。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: decomposition-guide.md

**Files:**
- Create: `claude/skills/pick-next/references/decomposition-guide.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。

```markdown
# decomposition-guide

`pick-next` の Step 6B（新規テーマ → spec/plan 書き出し）で sub-issue を分解するときの指針。

## 階層は 1 段固定

親 Issue + sub-issue のみ。**sub-issue の sub-issue は作らない**。`create-issue` の現状仕様と整合する。

## 粒度の目安

1 sub-issue = **半日〜2 日 / PR 1 本**。

## 判断ロジック

選定したテーマの **コスト軸スコア**（`score-axes.md` 参照）で分岐:

| コスト | sub-issue 件数 | 補足 |
|--------|--------------|------|
| 小 (1d 以下) | 0 件（親 Issue だけ） | 「これを 2 つ以上に切る価値はないですね、親だけにします」と提案 |
| 中 (2-5d) | 2〜4 件 | 標準的な分解 |
| 大 (1w+) | エスカレーション提案 | 「このスキルの守備範囲を超えてる気配。`superpowers:brainstorming` 本体に切り替えますか？」と確認 |

ユーザーが「コスト大でも `pick-next` で進めたい」と返した場合は、件数を 5〜8 件まで許容する。それでも収まらないなら強制的にエスカレーション。

## 各 sub-issue に必要な情報

最低限以下を埋める:

- **タイトル**（25 文字以内、imperative）
- **概要**（1〜3 行）
- **変更対象ファイル**（推測でも書く）
- **受入条件**（チェックリスト 2〜5 項目、テストを含む）
- **依存関係**（前ステップ完了が必要なら明記）

## 依存関係の扱い

plan に「Step B は Step A の後」と書かれていれば、`create-issue` 側で `blocks` / `blocked by` として登録される。記述例:

```
**依存**: Step 1 完了後
```

または:

```
**依存**: なし（並列実行可）
```

## 1 件しか出ない場合の提案

Step 6B でユーザーと分解を進めた結果、sub-issue が 1 件にしかならなかった場合:

> 「sub-issue が 1 件しか出ませんでした。親 Issue だけで十分では？」

ユーザーが OK したら sub-issue を作らず、親 Issue 1 本だけで `create-issue` を呼ぶ。plan も「ステップ 1: <作業全体>」と書くだけで OK。
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/decomposition-guide.md
```

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/decomposition-guide.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next decomposition-guide.md を追加

sub-issue 分解の指針: 階層 1 段固定、半日〜2 日粒度、コスト軸
スコアによる件数判定 (小→0、中→2-4、大→エスカレーション提案)、
各 sub-issue の必要情報、1 件しか出ない場合の親のみ提案を定める。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: spec-template.md

**Files:**
- Create: `claude/skills/pick-next/references/spec-template.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。バックティック 3 連で囲まれた spec テンプレ部分は、ファイル内では 4 連バックティックで囲んで実装する。

```markdown
# spec-template

`pick-next` の Step 6B で `docs/superpowers/specs/<date>-<slug>-design.md` に書き出す軽量 spec のテンプレ。

## ファイル名規則

- 日付: `date +%Y-%m-%d`（書き出し時点の今日の日付）
- slug: タイトルから ASCII 化した kebab-case（例: `auth-rate-limit-improve`）
- 拡張子: `-design.md`

例: `docs/superpowers/specs/2026-05-14-auth-rate-limit-improve-design.md`

## テンプレ全文

````markdown
---
labels: [feature]
pick-next: true
---

# <タイトル>

## 目的・背景

<5〜10 行。「なぜ今これか」「何の問題を解くか」を明記。
3 軸スコアの根拠もここに含める。>

## 採用判断（3 軸スコア）

- インパクト: 高 (3) — <理由>
- モチベーション: 高 (3) — <理由>
- コスト: 中 (2-5d) — <内訳の概算>

## 受入条件

- [ ] <ユーザー視点で完了が確認できる条件>
- [ ] <技術的な条件>
- [ ] <テストが追加されている>

## スコープ外

- <今回はやらないこと>
- <将来の拡張ポイント>

## 却下した代替案

| 候補 | インパクト | モチベ | コスト | 却下理由 |
|------|-----------|--------|--------|----------|
| A: ... | 高 | 中 | 中 | <理由> |
| C: ... | 高 | 低 | 中 | <理由> |

## アプローチ（任意、必要なら）

<技術的な方針が複数ある場合、選んだ方針と理由>
````

## フィールドの意味

- **labels**: `create-issue` がそのまま Issue ラベルに転用する。デフォルト `[feature]`
- **pick-next: true**: 中断再開検出と `--history` 一覧で識別子として使う。**必須**
- **目的・背景**: `create-issue` が親 Issue 本文の「## 目的・背景」に転載
- **採用判断（3 軸スコア）**: `pick-next` 固有の追加情報。`create-issue` は本文の補足として転載
- **受入条件**: `create-issue` が親 Issue 本文の「## 受入条件」に転載
- **スコープ外**: 親 Issue 本文に転載
- **却下した代替案**: `pick-next` 固有。`create-issue` は本文末尾に転載
- **アプローチ**: あれば `create-issue` が「設計コメント」として親 Issue にコメント追加

## 記入例

````markdown
---
labels: [feature, security]
pick-next: true
---

# auth-rate-limit-improve: ログイン API のレート制限を強化

## 目的・背景

現状 `/api/auth/login` には IP ベースのレート制限が無く、ブルート
フォース攻撃に対して脆弱。直近のログ監査で同一 IP からの大量試行
を確認しており、優先度を上げて対応する。

## 採用判断（3 軸スコア）

- インパクト: 高 (3) — セキュリティリスクの直接緩和
- モチベーション: 中 (2) — やる必要は感じるが心は躍らない
- コスト: 中 (3d) — middleware 追加 + テスト + 本番ロールアウト

## 受入条件

- [ ] 同一 IP からの login 試行が 1 分間に 5 回を超えると 429 を返す
- [ ] レート制限カウンタは Redis に保存される（in-memory 不可）
- [ ] 統合テストで 429 動作を確認できる
- [ ] エラーレスポンスに retry-after ヘッダが含まれる

## スコープ外

- ログイン以外のエンドポイントのレート制限（次回テーマ）
- captcha 統合（コスト過大、別 PR）

## 却下した代替案

| 候補 | インパクト | モチベ | コスト | 却下理由 |
|------|-----------|--------|--------|----------|
| プロフィール画面の刷新 | 中 | 高 | 中 | 楽しいがリスク低、後回しでよい |
| SEO メタタグ最適化 | 高 | 低 | 小 | モチベ低、心が乗らないので先送り |

## アプローチ

express-rate-limit + Redis ストア。`@nestjs/throttler` も検討した
が、middleware 単体で済むほうが依存が軽いため express-rate-limit
を採用。
````
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/spec-template.md
grep -c '^---$' /Users/goto/.dotfiles/claude/skills/pick-next/references/spec-template.md
```

Expected: ファイル存在、`---` 区切り行が 4 回（テンプレ本体 1 組 + 記入例 1 組）以上現れる。

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/spec-template.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next spec-template.md を追加

軽量 spec のフロントマター (labels, pick-next: true) と 6 セクション
(目的・背景、採用判断、受入条件、スコープ外、却下した代替案、
アプローチ) のテンプレを定義する。記入例も併記し、create-issue が
転載する箇所を明示する。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: plan-template.md

**Files:**
- Create: `claude/skills/pick-next/references/plan-template.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。

```markdown
# plan-template

`pick-next` の Step 6B で `docs/superpowers/plans/<date>-<slug>.md` に書き出す軽量 plan のテンプレ。

## ファイル名規則

- 日付・slug は spec と同じ（spec-template.md 参照）
- 拡張子: `.md`（`-design` は付けない）

例: `docs/superpowers/plans/2026-05-14-auth-rate-limit-improve.md`

## テンプレ全文

````markdown
# <タイトル> - 実装プラン

## ステップ概要

1. <ステップ 1 タイトル> （0.5d）
2. <ステップ 2 タイトル> （1d）
3. <ステップ 3 タイトル> （0.5d）

## ステップ詳細

### Step 1: <タイトル>

**変更対象**:
- `path/to/file_a.ts`
- `path/to/file_b.ts`

**受入条件**:
- [ ] <ステップ固有の完了条件>
- [ ] テストが追加されている

**依存**: なし

### Step 2: <タイトル>

**変更対象**:
- `path/to/file_c.ts`

**受入条件**:
- [ ] ...

**依存**: Step 1 完了後

## 検証手順（任意、必要なら）

- <手動テスト手順>

## ロールバック方針（任意、DB マイグレ等）

- <あれば書く>
````

## sub-issue が 1 件のときのテンプレ

`decomposition-guide.md` の通り、sub-issue が 1 件しか出ない場合は以下のように 1 ステップだけ書く。

````markdown
# <タイトル> - 実装プラン

## ステップ概要

1. <作業全体> （Xd）

## ステップ詳細

### Step 1: <作業全体>

**変更対象**:
- <該当ファイル>

**受入条件**:
- [ ] <親 Issue の受入条件と同じで OK>
- [ ] テストが追加されている

**依存**: なし
````

## sub-issue が 0 件（親 Issue だけ）のときのテンプレ

コスト「小」のテーマでは sub-issue ゼロ。plan は省略してもよいが、`create-issue` のセルフレビューを通すため最小限の plan を書き出す。

````markdown
# <タイトル> - 実装プラン

## ステップ概要

1. <作業全体> （Xd）

このテーマは sub-issue 分割なしで親 Issue 1 本で扱う。

## ステップ詳細

### Step 1: <作業全体>

**変更対象**:
- <該当ファイル>

**受入条件**:
- [ ] <親 Issue の受入条件と同じ>

**依存**: なし
````

## フィールドの意味

- **ステップ概要**: 番号付きリスト。`create-issue` が親 Issue 本文の「## サブタスク」に転載する
- **Step N の変更対象**: `create-issue` が sub-issue 本文の「## 変更対象」に転載
- **Step N の受入条件**: sub-issue 本文の「## 受入条件」に転載
- **Step N の依存**: `create-issue` が `blocks` / `blocked by` として登録
- **検証手順 / ロールバック方針**: あれば親 Issue 本文末尾に追記
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/plan-template.md
```

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/plan-template.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next plan-template.md を追加

軽量 plan のテンプレを定義する。標準形 (sub-issue 2+ 件)、
sub-issue 1 件のとき、sub-issue 0 件 (親のみ) の 3 パターンを
カバーし、create-issue が転載するフィールドの対応関係も明示する。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: decision-log-template.md

**Files:**
- Create: `claude/skills/pick-next/references/decision-log-template.md`

- [ ] **Step 1: ファイルを書き出す**

Write ツールで以下を書く。

```markdown
# decision-log-template

`pick-next` の Step 6C（保留分岐）で、ユーザーが「判断ログを残す」を選んだ場合に書き出すファイルのテンプレ。

## ファイル名規則

- 日付: `date +%Y-%m-%d`
- 拡張子: `-pick-next-skip.md`

例: `docs/superpowers/decisions/2026-05-14-pick-next-skip.md`

同日に複数の保留判断がある場合は連番を付ける: `2026-05-14-pick-next-skip-2.md`

## テンプレ全文

````markdown
---
date: 2026-05-14
type: pick-next-skip
---

# <セッションのテーマ要約> - 保留判断

## 検討した候補

| # | 候補 | タイプ | インパクト | モチベ | コスト | 推奨理由 / コメント |
|---|------|-------|-----------|--------|--------|--------------------|
| 1 | ... | 既存 | 高 | 中 | 中 | ... |
| 2 | ... | 新規 | 中 | 高 | 低 | ... |
| 3 | ... | 既存 | 高 | 低 | 中 | ... |

## 保留した理由

<なぜ今は何もやらないのか。例:
- 全候補のモチベが低く、無理にやると進捗しない
- 直近で別タスクのリリースが控えており、新規着手は控える
- 候補がどれも spec 詰まっておらず、もう少し情報を集める必要がある>

## 次回検討タイミング

<具体的な日付や条件があれば記入。例: 「2026-05-21 のリリース後」「Issue X が解消したら」>

## メモ

<その他、後で読み返したい情報>
````

## 設計意図

- **decisions/ ディレクトリ**: spec/plan とは別ディレクトリに分けることで、`create-issue` の重複チェック（spec タイトル一致を見る）と混ざらない
- **type: pick-next-skip**: 将来的に他の判断ログ（例: 仕様変更ログ）と区別できるよう、明示的にタイプを持つ
- **3 軸スコアを残す**: 「なぜ保留したか」を後から辿れるようにする。`--review` の入力データとしても使う
- **次回検討タイミング**: 任意項目。書かなくても良いが、書いてあると将来の `pick-next` 起動時に自動で「前回保留したやつ、検討タイミング来てます」と案内できる（将来拡張）

## docs/superpowers/decisions/ ディレクトリ

ディレクトリが存在しない場合、Step 6C で書き出す前に `mkdir -p docs/superpowers/decisions` を実行する。
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/references/decision-log-template.md
```

- [ ] **Step 3: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/references/decision-log-template.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next decision-log-template.md を追加

「保留」分岐 (Step 6C) で書き出す判断ログのテンプレを定義する。
docs/superpowers/decisions/ 配下に分離し、3 軸スコア・保留理由・
次回検討タイミングを残す。--review の入力データとしても使う。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: SKILL.md 本文を完成

**Files:**
- Modify: `claude/skills/pick-next/SKILL.md`

- [ ] **Step 1: SKILL.md を完成版で上書き**

Write ツールで `/Users/goto/.dotfiles/claude/skills/pick-next/SKILL.md` を以下の完全版で上書きする。

```markdown
---
name: pick-next
description: 「次に何をやるか」を対話で決定する。既存 active issue（Linear / GitHub）の優先度推奨、新規テーマの候補出しと 3 軸スコア比較、判断結果に応じて Issue 作成・既存 Issue 選定・保留の 3 分岐に振り分ける。「次に何やる？」「Linear/GitHub 確認して」「優先度教えて」「次の開発内容を相談したい」と聞かれたら必ず使う。引数なし or 任意のヒント文字列で起動。
argument-hint: '[hint] [--epic <issue-id>] [--all] [--axes <カスタム軸>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
  - Write
  - TaskCreate
  - TaskUpdate
  - ToolSearch
  - Skill
---

# pick-next

「次に何をやるか」を対話で決定する。終端は 3 つの分岐に分かれる:

1. **既存 active issue を選ぶ** → Issue 番号を提示、`/issue-dev <番号>` を案内して終了
2. **新規テーマを作る** → 軽量 spec/plan を書き出し → `Skill: create-issue` で親 + sub-issue を登録
3. **今はやらない / 保留** → 何もしない（判断ログは任意）

## このスキルがしないこと

- 実装の着手（`issue-dev` の責務）
- フル spec / plan の作成（`superpowers:brainstorming` の責務）
- ロードマップ全体の管理（`service-design-builder` の責務）
- Issue 登録の生 API 呼び出し（`create-issue` に委譲）

## 引数

```
/pick-next [hint] [--epic <issue-id>] [--all] [--axes <カスタム軸>] [--history] [--review]
```

- `[hint]`: 任意の方向性ヒント（例: `認証周り強化したい`）
- `--epic <issue-id>`: 特定 epic の sub-issue に既存候補を限定
- `--all`: 既存候補の上位 5 件で打ち切らず全件表示
- `--axes <カスタム軸>`: 3 軸を上書き（例: `インパクト,学習,コスト`）
- `--history`: 過去の `pick-next` セッション一覧を表示して終了
- `--review`: 過去 spec の予想コスト vs 実際所要時間を集計表示して終了

## 対話プロトコル

### Step 0: 環境検出 + handover

`references/environment-detection.md` を参照して並列実行する。
得られる値: `REPO_ROOT` / `BRANCH` / `TRACKER_TYPE` / `HANDOVER_NOTES` / `RECENT_COMMITS` / `RESUMING_SPEC`。

`RESUMING_SPEC` が見つかったら「続きから / 破棄」を確認し、再開なら Step 5 から続行。

### Step 1: 既存 active issue 取得 & ランク付け

`references/ranking-signals.md` を参照。

- `TRACKER_TYPE = linear`: `ToolSearch` で MCP ツールを読み込んで `mcp__linear-server__list_issues` を呼ぶ
- `TRACKER_TYPE = github`: `gh issue list --state open --assignee @me --limit 30` を呼ぶ
- 空: スキップ、Step 2 へ

シグナル加点で上位 3〜5 件を「既存候補」として保持。**スコアは表示せず**、推奨理由を自然言語で持つ。

### Step 2: ヒアリング & 新規候補引き出し

引数 `[hint]` があれば起点に。最大 3 質問:

1. 直近の状況（直近マージ PR、残ってる TODO）
2. 頭にある候補（既に「やりたい」と思ってること）
3. 制約（期限、使える時間、避けたいこと）

過剰なヒアリングは避ける。1 メッセージにつき 1 質問が原則。

### Step 3: 候補統合

| 出所 | 件数の目安 |
|-----|----------|
| 既存候補（Step 1） | 上位 3 件 |
| 新規候補（Step 2） | ユーザー言及候補 |
| 推測候補（コードベース TODO / git log） | 0〜2 件 |

合計 **2〜5 件**。多すぎ → 圧縮、1 件しかない → 「比較対象がない選択は判断にならない」と促す。

各候補に **タイプ**（既存 / 新規 / 推測）をラベリング。

### Step 4: 3 軸スコア & 比較

`references/score-axes.md` を参照。比較表を提示:

```
| # | 候補 | タイプ | インパクト | モチベ | コスト | 推奨理由 / コメント |
|---|------|--------|-----------|--------|--------|--------------------|
```

判定原則: **コストが許容範囲なら、インパクト × モチベの積が高いものを選ぶ**。モチベ低を機械的に選ばない。

`--axes` 引数があれば軸を上書き。

### Step 5: 1 つ選定 & 分岐判定

ユーザーが 1 つ選ぶ。スキル側で **タイプ** を見て分岐:

- 既存 → Step 6A
- 新規 / 推測 → Step 6B
- ユーザーが「今はやらない」 → Step 6C

### Step 6A: 既存 Issue 確定

```markdown
## 確定: <Issue 番号> - <タイトル>

着手するには:
  /issue-dev <Issue 番号>

または直接 worktree を切る場合:
  worktrunk <Issue 番号>
```

何も書き出さず Step 7 へ。

### Step 6B: spec/plan 書き出し → create-issue

`references/spec-template.md` `references/plan-template.md` `references/decomposition-guide.md` を参照。

```bash
DATE=$(date +%Y-%m-%d)
SLUG=<タイトルから ASCII kebab-case>
SPEC=docs/superpowers/specs/${DATE}-${SLUG}-design.md
PLAN=docs/superpowers/plans/${DATE}-${SLUG}.md
```

Write ツールで両ファイルを書き出す。**git commit は自動実行しない**、ユーザーに案内のみ。

その後:

```
Skill: create-issue
args: <SPEC> <PLAN>
```

`create-issue` がエラーで停止した場合（tracker 未設定等）、`pick-next` 側で「`.claude/project.yml` を設定してから `/create-issue <SPEC> <PLAN>` を手動実行」と案内して終了。spec/plan ファイルは残す。

### Step 6C: 保留 (判断ログ任意)

`references/decision-log-template.md` を参照。

ユーザーに「判断ログを残す？」と確認:

- **残す** → `docs/superpowers/decisions/${DATE}-pick-next-skip.md` に書き出す。ディレクトリが無ければ `mkdir -p` する
- **残さない** → 何もしない

### Step 7: 完了報告

3 分岐共通フォーマット:

```markdown
## ✅ pick-next 完了

**選定結果:** <既存 KISSA-XX / 新規テーマ / 保留>
**理由:** <推奨理由 or 3 軸スコア要約>

### 比較した候補
| # | 候補 | タイプ | スコア | 結論 |
|---|------|--------|--------|------|
| 1 | ... | 既存 | ★ 採用 | ... |
| 2 | ... | 新規 | 却下 | <理由> |

### 生成物（新規分岐の場合のみ）
- Spec: <path>
- Plan: <path>
- 親 Issue: <番号>
- Sub-issue: N 件

### 次のステップ
<分岐に応じた案内>
```

## 失敗時の挙動

| 状況 | 挙動 |
|------|------|
| repo 外で起動 | コードベース読込みスキップ、対話続行 |
| tracker 未設定 | 既存候補ゼロで続行、6B 選択時に案内して停止 |
| Linear MCP 認証エラー | 案内して既存候補ゼロで続行 |
| GitHub `gh auth status` NG | 案内して既存候補ゼロで続行 |
| ユーザーが「やめる」 | 何もしない |
| 中断再開検出 | 「続きから / 破棄」確認 |
| sub-issue 分解で「全部 1 件にできない」 | `superpowers:brainstorming` へエスカレーション提案 |
| `create-issue` で重複検出 | 停止メッセージ表示、spec/plan は残す |

## オプション機能（`--history` `--review`）

### `--history`

`docs/superpowers/specs/*-design.md` をスキャンし、フロントマターに `pick-next: true` を含むものを日付降順で一覧表示して終了。

```
## pick-next 過去セッション一覧

| 日付       | タイトル                   | 親 Issue |
|------------|---------------------------|---------|
| 2026-05-14 | auth-rate-limit-improve   | KISSA-72 |
| 2026-05-07 | coffee-log-export         | KISSA-65 |
```

### `--review`

過去 spec の `採用判断（3 軸スコア）` の `コスト` と、関連 PR の作成 → マージ時間を比較する。出力は表のみ:

```
| 日付       | タイトル                | 予想コスト | 実際 (PR 作成 → マージ) | 差分 |
|------------|------------------------|-----------|------------------------|------|
| 2026-05-14 | auth-rate-limit-improve | 中 (3d)   | 4d                     | +1d |
| 2026-05-07 | coffee-log-export      | 小 (1d)   | 1d                     | 0    |
```

自動学習はしない。ユーザーが目視で「自分の見積もりにバイアスがある」と気づく材料を提供するだけ。

## 設計判断

- **スコアは内部のみ vs 表示**: 既存 issue ランキング = 内部のみ（数値見せると無駄な議論）/ 3 軸スコア = 表示（ユーザーが選ぶための材料）
- **モチベ低を機械的に選ばない**: 個人開発はモチベが落ちると進まない
- **git commit は自動実行しない**: spec/plan 書き出し後、ユーザーに案内のみ。誤コミット防止
- **`linear-next` 機能内包**: `pick-next` 安定後に `linear-next` は別 PR で削除予定
```

- [ ] **Step 2: ファイル確認**

```bash
ls -l /Users/goto/.dotfiles/claude/skills/pick-next/SKILL.md
wc -l /Users/goto/.dotfiles/claude/skills/pick-next/SKILL.md
```

Expected: 250〜350 行程度。

- [ ] **Step 3: 全 references が SKILL.md から参照されているか確認**

```bash
for f in environment-detection ranking-signals score-axes decomposition-guide spec-template plan-template decision-log-template; do
  grep -q "references/${f}.md" /Users/goto/.dotfiles/claude/skills/pick-next/SKILL.md \
    && echo "OK: ${f}" \
    || echo "MISS: ${f}"
done
```

Expected: 全 7 行が `OK: ...` で出力される。

- [ ] **Step 4: コミット**

```bash
cd /Users/goto/.dotfiles
git add claude/skills/pick-next/SKILL.md
git commit -m "$(cat <<'EOF'
feat(skills): pick-next SKILL.md 本文を完成

スケルトンだった SKILL.md を本文で上書きする。Step 0〜7 の対話
プロトコル、3 分岐 (既存 / 新規 / 保留) のロジック、--history
--review --axes フラグの仕様、失敗時挙動表、設計判断を盛り込む。
references 全 7 ファイルへの参照リンクも整備する。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: dotfiles の CLAUDE.md に追記

**Files:**
- Modify: `CLAUDE.md` (「Claude Code 設定」セクション)

- [ ] **Step 1: 追記箇所を特定**

```bash
grep -n 'claude/skills/' /Users/goto/.dotfiles/CLAUDE.md | head -20
```

Expected: 既存のスキル説明（`handover/`, `feature-team/`, `create-issue/` 等）の行番号がリストアップされる。

- [ ] **Step 2: `create-issue` の説明の直後に `pick-next` を追記**

Edit ツールで `/Users/goto/.dotfiles/CLAUDE.md` の該当箇所を編集する。

`create-issue` の説明行（`- claude/skills/create-issue/ は ...` で始まる行）の直後に以下の 1 行を追加する。

old_string（`create-issue` 説明の末尾）:
```
- `claude/skills/create-issue/` は spec/plan を入力に Linear / GitHub の親 Issue + sub-issue を自律登録するスキル。引数 `<spec-path> <plan-path>` で受け取り、tracker は `.claude/project.yml` の `tracker.type` から自己解決する。`feature-team` Phase 2 から呼ばれる前提
```

new_string（`pick-next` 説明を 1 行追加）:
```
- `claude/skills/create-issue/` は spec/plan を入力に Linear / GitHub の親 Issue + sub-issue を自律登録するスキル。引数 `<spec-path> <plan-path>` で受け取り、tracker は `.claude/project.yml` の `tracker.type` から自己解決する。`feature-team` Phase 2 から呼ばれる前提
- `claude/skills/pick-next/` は「次に何をやるか」を対話で決定するスキル。既存 active issue（Linear / GitHub）の優先度推奨と、新規テーマの 3 軸スコア比較を統合し、結果に応じて Issue 作成・既存 Issue 選定・保留の 3 分岐に振り分ける。`linear-next` の機能を内包しており、安定後に `linear-next` は削除予定
```

注意: 既存の `create-issue` 説明文と完全一致しない場合は、Read で実際の文言を確認してから Edit する。

- [ ] **Step 3: 追記後の差分を確認**

```bash
cd /Users/goto/.dotfiles
git diff CLAUDE.md
```

Expected: 1 行追加のみ（`pick-next` の説明）。他の行に変更がないこと。

- [ ] **Step 4: コミット**

```bash
cd /Users/goto/.dotfiles
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(claude): pick-next スキルへの言及を追加

CLAUDE.md「Claude Code 設定」セクションに pick-next スキルの
説明を追加する。linear-next 機能の内包と将来的な削除予定にも
触れる。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: スモーク検証

**Files:** なし（手動検証のみ）

- [ ] **Step 1: symlink 経由でスキルファイルが見えることを確認**

```bash
ls -la /Users/goto/.claude/skills/pick-next/
ls -la /Users/goto/.claude/skills/pick-next/references/
```

Expected: SKILL.md と 7 つの references が symlink 経由で表示される。

- [ ] **Step 2: SKILL.md のフロントマターをパースできることを確認**

```bash
head -15 /Users/goto/.claude/skills/pick-next/SKILL.md
```

Expected: `---` で囲まれたフロントマターが正しく表示され、`name: pick-next` `argument-hint:` `allowed-tools:` が読める。

- [ ] **Step 3: 新セッションを開いて `/pick-next` で起動確認**

ユーザーに以下を依頼:

> 新しい Claude Code セッションを開いて `/pick-next` を実行してください。SKILL.md が読み込まれ、Step 0（環境検出）から始まることを確認します。

Expected: スキルが起動し、Step 0 の環境検出コマンド（`git rev-parse --show-toplevel` 等）が実行される。

- [ ] **Step 4: social coffee note のリポジトリで実走（受入条件 §15 の最終検証）**

ユーザーに以下を依頼:

> social coffee note のリポジトリで `/pick-next` を実走し、以下を観察してください:
>
> 1. 既存 active issue（Linear）が取得できるか
> 2. 候補出し（最大 5 件）が出るか
> 3. 3 軸スコアの対話がスムーズか
> 4. 分岐判定（既存 / 新規 / 保留）が機能するか
> 5. 新規分岐選択時、`docs/superpowers/specs/` `docs/superpowers/plans/` にファイルが書き出され、`Skill: create-issue` が呼ばれるか

問題があれば `feedback_*.md` / `project_*.md` として auto memory に記録する。

- [ ] **Step 5: スモーク検証完了の合図としてこの Task をチェック**

何もコミットしない（検証のみ）。問題が見つかれば該当 Task に戻って修正する。

---

## Self-Review

このプランを spec と照合する。

### 1. Spec coverage

spec §15 の受入条件 11 項目をプランタスクに対応させる。

| spec §15 受入条件 | カバーする Task |
|-----------------|---------------|
| `claude/skills/pick-next/SKILL.md` と 7 つの references が配置 | Task 1 (skeleton) + Task 2-8 (references) + Task 9 (本文) |
| `/pick-next` で起動し、Step 0〜7 動作 | Task 11 Step 3 |
| Linear MCP 経由で issue 取得 | Task 3 (ranking-signals.md) + Task 11 Step 4 |
| GitHub gh CLI 経由で issue 取得 | Task 3 (ranking-signals.md) + Task 11 Step 4 |
| tracker 未設定で新規候補のみ動作 | Task 2 (environment-detection.md) + Task 11 Step 4 |
| 中断再開検出 | Task 2 (environment-detection.md) |
| 6B で軽量 spec/plan が `docs/` に書き出される | Task 6 (spec-template.md) + Task 7 (plan-template.md) + Task 9 (Step 6B) |
| 6B から `create-issue` が `Skill` 経由で呼ばれる | Task 9 (Step 6B) |
| handover 未消費メモが +100 加点 | Task 3 (ranking-signals.md) |
| `CLAUDE.md` に新スキル言及追加 | Task 10 |
| `linear-next` は本 PR では触らない | spec §13 で明示、Task 一覧に含めない（OK） |

→ 全 11 項目カバー、漏れなし。

### 2. Placeholder scan

- 「TBD」「TODO」「実装後で書く」「fill in details」「append later」「適切なエラー処理を追加」「validation 追加」は本プラン本文には現れない（reference 内の例文に「<タイトル>」「<理由>」のような **テンプレ用プレースホルダー**は含むが、これは意図したテンプレ構文）
- 各 Step の中身は Write/Edit/Bash の具体内容を書ききっている

→ 問題なし。

### 3. Type consistency

- ファイルパスは全 Task で `/Users/goto/.dotfiles/claude/skills/pick-next/...` の絶対パス、または相対パス `claude/skills/pick-next/...` で一貫
- フロントマターの `pick-next: true` は spec-template.md / environment-detection.md（中断再開検出） / SKILL.md（`--history`）の 3 箇所で同一表記
- `tracker.type` の値（`linear` / `github` / 空）は environment-detection.md と SKILL.md で一致
- 3 軸の値（高 (3) / 中 (2) / 低 (1)、コストは小/中/大）は score-axes.md と spec-template.md で一致
- ランキングシグナルの加点値（+100, +50, +30, +40, +20, +10, -50, -10）は ranking-signals.md と spec の §6.2 で一致

→ 問題なし。

---

## 実行方法

このプランは **subagent-driven-development** または **executing-plans** で実行できる。

- **subagent-driven-development**: Task ごとに fresh subagent を dispatch、Task 間で人間レビュー
- **executing-plans**: このセッション内でタスクを順次実行、checkpoint で人間レビュー

dotfiles 改修なので worktree を切ってから実装する。`superpowers:using-git-worktrees` または `worktrunk` を使う。
